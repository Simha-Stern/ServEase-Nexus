module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    // setupFiles: ['./test/setup.ts'], // Add a setup file if needed
    // testPathIgnorePatterns: ['/node_modules/', '/dist/'],
  };
  